<UserControl x:Class="morphological_image_processing_wpf.View.Components.DisplayImage.DisplayImageComponent"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:morphological_image_processing_wpf.View.Components.DisplayImage"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="600">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image x:Name="BeforeImage" Grid.Row="0" Grid.Column="0" Margin="10, 10, 10, 10" />
        <Button x:Name="BeforeImageDownload" Grid.Row="1" Grid.Column="0" Width="100" Height="40" Content="Save To Disk" Click="BeforeImageDownload_Click"/>
        <Image x:Name="AfterImage" Grid.Row="0" Grid.Column="1" Margin="10, 10, 10, 10"/>
        <Button x:Name="AfterImageDownload" Grid.Row="1" Grid.Column="1" Width="100" Height="40" Content="Save To Disk" Click="AfterImageDownload_Click"/>
    </Grid>
</UserControl>
